name: ci-for-demo

on:
  push:
    branches:
      - "main"

jobs:
  call-workflow:
    uses: bimitshah/workflows/.github/workflows/test.yaml@main
  trigger-workflow:
    runs-on: ubuntu-latest
    steps:
      - id: print
        run: |
          echo ${{ steps.build_wrapper.outputs.name }}
          echo ${{ jobs.call-workflow.outputs.name_wrapper }}
