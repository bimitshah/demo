name: ci-for-demo

on:
  push:
    branches:
      - "main"
      - "feature/PPAC-89"
  release:
    types:
      - "published"

jobs:
  trigger-workflow:
    outputs:
      image: ${{ steps.build_wrapper.outputs.image }}
    uses: bimitshah/workflows/.github/workflows/test.yaml@main
